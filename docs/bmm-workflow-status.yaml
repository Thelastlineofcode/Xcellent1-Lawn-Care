# BMM Workflow Status - Normalized (created 2025-12-09)
# This file was generated from `workflow-init` defaults for a brownfield BMad Method track.
generated: "2025-12-09T00:00:00Z"
project: "Xcellent1 Lawn Care"
project_type: "lawncare"
selected_track: "method"
field_type: "brownfield"
workflow_path: "bmad/bmm/workflows/workflow-status/workflow.yaml"

workflow_status:
  # Brownfield prerequisites
  document-project:
    status: skipped
    notes: "Project has planning docs (STORIES, epics, other specs). No additional docgen required."

  # Optional Discovery (User-driven)
  brainstorm-project:
    status: optional
  research:
    status: optional
  product-brief:
    status: optional

  # Planning
  prd:
    status: docs/PRD.md
    notes: "PRD created at docs/PRD.md on 2025-12-09. Review and adjust success metrics as needed."
  validate-prd:
    status: optional
  create-design:
    status: conditional
    condition: "if_has_ui"

  # Solutioning
  create-architecture:
    status: docs/ARCHITECTURE.md
    notes: "Architecture doc created at docs/ARCHITECTURE.md on 2025-12-10. Review and expand as needed."
  validate-architecture:
    status: docs/ARCHITECTURE.md
    notes: "Architecture document drafted — review and validation recommended."
  solutioning-gate-check:
    status: done
    notes: "Gate check completed — architecture documented and validated as of 2025-12-10."

  # Implementation (phase 4) - sprint-status.yaml will control implementation tracking
  sprint-planning:
    status: docs/sprint-status.yaml
    notes: "Sprint created at docs/sprint-status.yaml on 2025-12-09. Use this file to track implementation progress."

# Existing implementation artifacts (for reference only — implementation statuses are tracked in sprint-status.yaml)
existing_artifacts:
  landing_page: web/static/home.html
  logo: web/static/images/logo.png
  server_redirect: server.ts
  dockerfile: Dockerfile
  deno_lock: deno.lock
  mcp_server: mcp-server

# Metadata: previous file archived for reference
previous_status_file: "docs/archive/bmm-workflow-status-2025-11-09.yaml"

notes: |
  - Workflow re-initialized to the BMad Method (brownfield) track on 2025-12-09.
  - Existing artifacts were preserved in the `existing_artifacts` section for context.
  - Implementation work will be tracked using `docs/sprint-status.yaml` created by the `sprint-planning` workflow.
