# Archive: LangChain Integration

Files in `bmad/agents/langchain-integration` were archived and copied into this folder for reference.
They are retained for historical or future use only and should not be included in production builds.

If you need to re-enable, copy back to `bmad/agents/langchain-integration/` and set `ENABLE_AI_PROTOTYPES=true` and re-run CI tests.
