langchain>=0.0.300
openai>=0.27.0
pytest>=7.0
httpx>=0.24.1

# This requirements file is archived and not used by default in CI. To enable,
# set `ENABLE_AI_PROTOTYPES=true` in your CI or development environment.
