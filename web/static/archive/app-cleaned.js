// Archived: app-cleaned.js (unused)
// Original: root-level app-cleaned.js - archived to avoid confusion with canonical `app.js`
// See `web/static/app.js` for canonical JS used across pages.

// Archive contents (original file saved for historical reference).
/* BEGIN ARCHIVE */
// Xcellent1 Lawn Care - Worker Recruitment & Field Management
// Designed for hiring lawn care workers and managing field operations
// VERSION: 2.0 - Cleaned and improved

const API_BASE = "";
const CONFIG = {
  pollInterval: window.config?.pollInterval ||
    parseInt(document.body.dataset.pollInterval || "20000"),
  maxFileSize: 5 * 1024 * 1024, // 5MB
};
/* (file truncated for archive - full copy preserved in repo history) */
/* END ARCHIVE */
